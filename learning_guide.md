# الدليل الشامل لفهم وتطوير مشروع Quizo 🎓🖥️

هذا الدليل مصمم ليأخذك من مستوى "مستخدم" إلى مستوى "مطور فاهم" لكل تفاصيل المشروع.

---

## 📖 الجزء الأول: هيكلة المشروع (وين ألاقي الملفات؟)

المشروع يتبع نمط **MVC** (Model-View-Controller) ولكن مع إضافة **Inertia.js** ليربط بين Laravel و React.

### 1. المسارات (Routes) 🛣️
المكان: `routes/web.php`
- هذا هو "موظف الاستقبال". أي رابط تفتحه في المتصفح (مثل `/login` أو `/admin/questions`) يتم تعريفه هنا.
- هو الذي يقرر أي "Controller" يجب أن يشتغل.

### 2. المتحكمات (Controllers) 🎮
المكان: `app/Http/Controllers/`
- هنا يقع "المنطق البرمجي". مثلاً: `QuizController.php` هو الذي يجلب الأسئلة من قاعدة البيانات ويرسلها للواجهة.

### 3. النماذج (Models) 🗄️
المكان: `app/Models/`
- هي حلقة الوصل مع جداول قاعدة البيانات. `Question.php` يمثل جدول الأسئلة.

### 4. الواجهات (Frontend - React) 🎨
المكان: `resources/js/Pages/`
- هنا تجد ملفات الـ React. كل ملف `.jsx` يمثل صفحة كاملة (مثل `Welcome.jsx` أو `Dashboard.jsx`).

---

## 🔗 الجزء الثاني: كيف يتم الربط؟ (Inertia.js السحرية)

عادة، المطورون يحتاجون لبناء API (JSON) ثم استقباله في React. **Inertia.js** اختصرت كل هذا:
1. الـ **Controller** في Laravel يجلب البيانات.
2. بدلاً من العودة بـ `view()`، يعود بـ `Inertia::render('PageName', ['data' => $data])`.
3. الـ **React Page** تستقبل الـ `data` كـ `props` وتعرضها فوراً.

---

## 🛠️ الجزء الثالث: شرح التقنيات بالتفصيل

### 1. Laravel (The Backend Framework)
- **Migrations**: هي "كود" يخبر قاعدة البيانات كيف تنشئ الجداول. (بدلاً من كتابة SQL يدوياً).
- **Eloquent**: هو نظام يجعلك تتعامل مع قاعدة البيانات كأنها كائنات برمجية. (بدلاً من `SELECT * FROM...` تكتب `Question::all()`).

### 2. React (The Frontend Library)
- **Components**: تقسم الواجهة لقطع صغيرة (زر، لوحة، قائمة).
- **Hooks (useState, useEffect)**: لإدارة حالة الصفحة (مثلاً: الانتقال للسؤال التالي).

### 3. Tailwind CSS (Styling)
- بدلاً من كتابة ملفات CSS طويلة، نكتب "Classes" مباشرة في HTML (مثل `bg-blue-500 text-white`).

---

## 🌍 الجزء الرابع: النشر والبيئة (Docker & Render)

### 1. ملف `.env` (الصندوق السري)
- يحتوي على كلمات المرور وروابط قواعد البيانات.
- **قاعدة ذهبية**: لا ترفع هذا الملف أبداً لـ GitHub!

### 2. Docker (الحاوية)
- ملف `Dockerfile` يخبر Render: "يا سيرفر، نزل PHP، ونزل Apache، ونزل الملحقات، ثم شغل المشروع". هذا يضمن أن المشروع يعمل بنفس الطريقة على أي جهاز.

### 3. Supabase (PostgreSQL)
- هي قاعدة بيانات سحابية. استخدمنا PostgreSQL لأنها معيار عالمي في النشر الاحترافي وتتحمل ضغطاً أكبر من MySQL التقليدية.

---

## 📈 الجزء الخامس: كيف تصبح محترفاً في هذا المشروع؟

1. **ابدأ بالـ Routes**: تتبع رابطاً معيناً من `web.php` وانظر إلى أي `Controller` يذهب.
2. **افهم الـ Controller**: انظر كيف يجلب البيانات وكيف يرسلها لـ `Inertia`.
3. **اففتح صفحة React المقابلة**: انظر كيف استقبلت البيانات وعرضتها باستخدام Tailwind.
4. **جرب التعديل**: غير نصاً في React، أو أضف حقلاً جديداً في قاعدة البيانات عبر Migration جديدة.

---
**تذكر**: البرمجة تُتعلم بالممارسة وليس فقط بالقراءة. حاول "تخريب" جزء من الكود ثم إصلاحه، فهذه هي الطريقة التي يفهم بها المحترفون!
